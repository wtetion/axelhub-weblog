JOB ID COPY FIX

The Accounts table now truncates long Job IDs visually, but the ⧉ button copies the COMPLETE Job ID to clipboard.
Hover the Job ID to see the full value.

No backend/API changes are required.
