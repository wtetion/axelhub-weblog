# Graph tab

The Web Log now includes a **Graph** tab with:
- Speed trend
- Estimated Money / s (computed from consecutive Money telemetry snapshots)
- Stolen Eggs accumulated during the current 30-minute window
- Account selector for one account or all accounts
- Automatic 30-minute graph reset and countdown

No extra frontend package is required; charts use the browser canvas.
