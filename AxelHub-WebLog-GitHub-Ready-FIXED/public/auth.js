(() => {
const bg=document.getElementById('bg'),ctx=bg.getContext('2d');let w=0,h=0,t=0;
const resize=()=>{w=bg.width=innerWidth;h=bg.height=innerHeight};addEventListener('resize',resize);resize();
(function draw(){t+=.004;ctx.fillStyle='#050609';ctx.fillRect(0,0,w,h);const g=ctx.createRadialGradient(w*.32,h*.25,10,w*.5,h*.5,Math.max(w,h)*.8);g.addColorStop(0,'rgba(255,208,73,.11)');g.addColorStop(.5,'rgba(255,65,77,.025)');g.addColorStop(1,'rgba(0,0,0,0)');ctx.fillStyle=g;ctx.fillRect(0,0,w,h);requestAnimationFrame(draw)})();
const T={en:{'login.title':'Welcome back','login.subtitle':'Sign in to your private Web Log.','login.submit':'Sign in','login.newHere':'New here?','login.create':'Create an account','signup.title':'Create account','signup.subtitle':'Each account gets its own private Web Log.','signup.submit':'Sign up','signup.usernameHint':'3–32 characters: letters, numbers, . _ -','signup.already':'Already have an account?','signup.signin':'Sign in','auth.username':'Username','auth.password':'Password','auth.confirmPassword':'Confirm password','err.username_taken':'Username already exists.','err.invalid_credentials':'Invalid username or password.','err.passwords_do_not_match':'Passwords do not match.','err.username_must_be_3_to_32_chars':'Username must be 3–32 allowed characters.','err.password_must_be_8_to_200_chars':'Password must be at least 8 characters.'},th:{'login.title':'ยินดีต้อนรับกลับ','login.subtitle':'เข้าสู่ระบบ Web Log ส่วนตัวของคุณ','login.submit':'เข้าสู่ระบบ','login.newHere':'ยังไม่มีบัญชี?','login.create':'สร้างบัญชี','signup.title':'สร้างบัญชี','signup.subtitle':'แต่ละบัญชีจะมี Web Log ส่วนตัวของตัวเอง','signup.submit':'สมัครสมาชิก','signup.usernameHint':'3–32 ตัวอักษร: ตัวอักษร ตัวเลข . _ -','signup.already':'มีบัญชีอยู่แล้ว?','signup.signin':'เข้าสู่ระบบ','auth.username':'ชื่อผู้ใช้','auth.password':'รหัสผ่าน','auth.confirmPassword':'ยืนยันรหัสผ่าน','err.username_taken':'ชื่อผู้ใช้นี้มีอยู่แล้ว','err.invalid_credentials':'ชื่อผู้ใช้หรือรหัสผ่านไม่ถูกต้อง','err.passwords_do_not_match':'รหัสผ่านไม่ตรงกัน','err.username_must_be_3_to_32_chars':'ชื่อผู้ใช้ต้องมี 3–32 ตัวอักษรตามรูปแบบที่กำหนด','err.password_must_be_8_to_200_chars':'รหัสผ่านต้องมีอย่างน้อย 8 ตัวอักษร'}};
let lang=localStorage.getItem('axelLang')||'en';const tr=k=>(T[lang]&&T[lang][k])||T.en[k]||k;
function apply(){document.documentElement.lang=lang;document.querySelectorAll('[data-i18n]').forEach(el=>el.textContent=tr(el.dataset.i18n));document.querySelectorAll('.auth-lang-btn').forEach(b=>b.classList.toggle('active',b.dataset.lang===lang));document.title=`Axel Hub • ${lang==='th'?(document.getElementById('loginForm')?'เข้าสู่ระบบ':'สมัครสมาชิก'):(document.getElementById('loginForm')?'Sign in':'Sign up')}`}
document.querySelectorAll('.auth-lang-btn').forEach(b=>b.addEventListener('click',()=>{lang=b.dataset.lang==='th'?'th':'en';localStorage.setItem('axelLang',lang);apply()}));apply();
async function post(url,body){const r=await fetch(url,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(body),credentials:'same-origin'});const d=await r.json().catch(()=>({}));if(!r.ok||!d.ok)throw new Error(d.error||`HTTP ${r.status}`);return d}
const form=document.getElementById('loginForm')||document.getElementById('signupForm'),error=document.getElementById('error');
form?.addEventListener('submit',async e=>{e.preventDefault();error.textContent='';const fd=new FormData(form),payload=Object.fromEntries(fd.entries()),btn=form.querySelector('button[type="submit"]');btn.disabled=true;try{if(form.id==='signupForm'){if(payload.password!==payload.confirm)throw new Error('passwords_do_not_match');delete payload.confirm;await post('/api/auth/signup',payload)}else await post('/api/auth/login',payload);location.href='/'}catch(err){error.textContent=tr('err.'+err.message).replace(/^err\./,'')||err.message}finally{btn.disabled=false}});
})();